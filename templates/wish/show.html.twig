{% extends 'base.html.twig' %}
{% block title %}
{{ wish.title }}
{% endblock %}
{% block body %}
    <div class="wishcontainer">
        <div class="wishdetails">
    <h1>{{ wish.title }}</h1>
            <h5><a href="{{ path('app_category_show', {'id':wish.category.id }) }}">{{ wish.category.name }}</a></h5>
    <p>{{ wish.description }}</p>
    <p>écrit par : {{ wish.author }} le : {{ wish.dateCreated | date("d/m/y ") }}</p>
        </div>
    </div>

    {% if app.user == wish.user %}
        <a type="button" href="{{ path('wish_edit', {'id': wish.id}) }}" class="btn btn-secondary">Edit</a>
    {% endif %}
{% endblock %}



